diff --git a/Users/michael/Projects/oss/graph-monitor/graph-web-app/src/components/DynamicChart.jsx b/Users/michael/Projects/oss/graph-monitor/graph-web-app/src/components/DynamicChart.jsx
index 1234567..abcdefg 100644
--- a/Users/michael/Projects/oss/graph-monitor/graph-web-app/src/components/DynamicChart.jsx
+++ b/Users/michael/Projects/oss/graph-monitor/graph-web-app/src/components/DynamicChart.jsx
@@ -111,7 +111,15 @@
   const { Chart, DataComponent } = chartComponents[config.type] || chartComponents.LineChart;
   const xAxisKey = config['x-axis'] === 'timestamp' ? 'timestamp' : config['x-axis'];
   const yAxisKey = config['y-axis'];
+  const metricKey = extractMetricKey(title);
 
+  // Get all unique keys from data for "all sites" charts
+  const dataKeys = metricKey === 'all' && data.length > 0 
+    ? Object.keys(data[0]).filter(key => key !== 'timestamp' && key !== xAxisKey)
+    : [yAxisKey];
+
+  const colors = ['#8884d8', '#82ca9d', '#ffc658', '#ff7c7c', '#8dd1e1', '#d084d0'];
+
   return (
     <div className="chart-container">
       <h3>{title}</h3>
@@ -127,13 +135,16 @@
           <YAxis />
           <Tooltip />
           <Legend />
-          <DataComponent
-            type="monotone"
-            dataKey={yAxisKey}
-            stroke="#8884d8"
-            fill="#8884d8"
-            strokeWidth={2}
-          />
+          {dataKeys.map((key, index) => (
+            <DataComponent
+              key={key}
+              type="monotone"
+              dataKey={key}
+              stroke={colors[index % colors.length]}
+              fill={colors[index % colors.length]}
+              strokeWidth={2}
+            />
+          ))}
         </Chart>
       </ResponsiveContainer>
     </div>